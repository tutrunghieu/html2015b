
x <- runif(10000, 0, 1);
y <- ifelse(x<0.3, "red", ifelse(x<0.4, "green", "blue"));
table(y)/10000


x <- runif(10000, 0, 1);
y <- ifelse(x<0.3, "red", ifelse(x<0.4, "green", "blue"));
table(y)/10000


x <- runif(10000, 0, 1);
y <- ifelse(x<0.3, "red", ifelse(x<0.4, "green", "blue"));
table(y)/10000
