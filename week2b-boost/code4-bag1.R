data(iris);

mr <- nrow(iris);

x <- as.integer( runif(100, 1, mr) );
print(x); cat("Summary:", min(x), max(x), mr, "\n");

x <- as.integer( runif(100, 1, mr) );
print(x); cat("Summary:", min(x), max(x), mr, "\n");

